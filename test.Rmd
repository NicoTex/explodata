---
title: "explodata"
author: "Texier-Dauby"
date: `r format(Sys.time(), "%d %B, %Y")`
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r dropbox}

# Load dataset from dropbox

library(tidyverse)
library(rdrop2)
drop_auth()


token <- drop_auth()
saveRDS(token, file = "token.rds")


drop_download("gilles_nico_tex/Selection_specimen_GAB_end_19022019.xlsx")

# dataset <- 
#   drop_read_csv("gilles_nico_tex/EOO.results.csv") %>% 
#   as_tibble()

dataset <- readxl::read_excel("Selection_specimen_GAB_end_19022019.xlsx")

```




## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
